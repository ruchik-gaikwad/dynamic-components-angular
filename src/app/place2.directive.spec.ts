import { Place2Directive } from './place2.directive';

describe('Place2Directive', () => {
  it('should create an instance', () => {
    const directive = new Place2Directive();
    expect(directive).toBeTruthy();
  });
});
