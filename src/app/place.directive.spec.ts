import { PlaceDirective } from './place.directive';

describe('PlaceDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceDirective();
    expect(directive).toBeTruthy();
  });
});
